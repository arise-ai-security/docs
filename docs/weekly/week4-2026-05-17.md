# Research Sync 2026-05-17

**Date:** 2026-05-17

---

## Summary

This week we finished the first three cells of the headline experiment matrix and produced the first cross-cell setup snapshot. **A1, A2, and B1 completed**; **C1 (Qwen worker) attempted but failed** and is currently un-investigated.

### Headline numbers

| Cell | Group | Architecture                                     | Dataset declared | Runs executed | Status |
|------|-------|--------------------------------------------------|-----------------:|--------------:|--------|
| A1   | A     | Flat Claude Code CLI, subagents ON                | 244              | 81            | complete |
| A2   | A     | Flat Claude Code CLI, subagents OFF               | 244              | 113           | complete |
| B1   | B     | Tree (boss → manager → worker), Judge OFF, Claude | 122              | 124 *(incl. 11 credit-failure reruns)* | complete |
| C1   | C     | Tree, Judge OFF, OpenHands + local Qwen worker    | 200              | 0             | **failed** |

The A1 vs A2 contrast isolates the contribution of CLI sub-agents under
an otherwise identical flat baseline. A vs B isolates our coordination
structure (boss → manager → worker) under an identical worker model
(Sonnet 4.5). C1 was supposed to isolate worker-model effects
(Qwen-via-OpenHands vs Claude) but didn't run.

---

## 1. Experiment setup (canonical reference)

Each cell extends `config/config.yaml` and overrides only the fields
relevant to its arm of the design.

```
  ┌─────────────────────────────────────────────────────────────────────────┐
  │                      Cell Architecture Overview                         │
  │                                                                         │
  │   A1 / A2  (FLAT)                  B1 / C1  (HIERARCHICAL)             │
  │   ┌──────────────────┐             ┌──────────────────────────────┐    │
  │   │ Claude Code CLI  │             │ BOSS (Sonnet 4.5)            │    │
  │   │ (single session) │             │  └─ MANAGER (Sonnet 4.5)     │    │
  │   │                  │             │      └─ WORKER (leaf exec)   │    │
  │   │ A1: Task ALLOWED │             │                              │    │
  │   │ A2: Task BLOCKED │             │ B1 worker: claude_code +     │    │
  │   │                  │             │            Sonnet 4.5        │    │
  │   │ Worker model:    │             │ C1 worker: openhands +       │    │
  │   │  Sonnet 4.5      │             │            qwen3:8b (Ollama) │    │
  │   └──────────────────┘             └──────────────────────────────┘    │
  └─────────────────────────────────────────────────────────────────────────┘
```

### 1.1 Per-cell configurations

| Field | A1 | A2 | B1 | C1 |
|---|---|---|---|---|
| `orchestration.mode` | flat | flat | hierarchical | hierarchical |
| `orchestration.skip_judge` | true | true | true | true |
| `boss.model` | *(unused in flat — schema requires)* | *(unused in flat)* | sonnet-4-5 | sonnet-4-5 |
| `manager.model` | *(unused in flat)* | *(unused in flat)* | sonnet-4-5 | sonnet-4-5 |
| `worker.tool` | claude_code | claude_code | claude_code | openhands |
| `worker.model` | sonnet-4-5 | sonnet-4-5 | sonnet-4-5 | ollama_chat/qwen3:8b |
| `worker.allowed_tools` | Read, Write, Edit, MultiEdit, Bash, Glob, Grep, **Task** | Read, Write, Edit, MultiEdit, Bash, Glob, Grep | Read, Write, Edit, MultiEdit, Bash, Glob, Grep | file_editor, glob, grep (+ MCP) |
| `worker.disallowed_tools` | WebSearch, WebFetch | WebSearch, WebFetch, **Task** | WebSearch, WebFetch | — |
| `worker.timeout` (s) | 5400 | 5400 | 3600 | 5400 |
| `worker.max_iterations_per_run` | 2500 | 2500 | 250 | 250 |
| `orchestration.max_run_duration_seconds` (s) | 7200 *(base)* | 7200 *(base)* | **5400** | **21600** |
| `orchestration.max_agent_step_seconds` (s) | 900 *(base)* | 900 *(base)* | 900 *(base)* | **1800** |
| `orchestration.tool_calling.*.max_iterations` | 20 | 20 | 20 | 20 |

Matrix runner parallelism (dispatch-time, not in cell config):

- A1/A2: `batch_size: 10`, `parallelism: 5` (batch driver)
- B1: batches of 12 at `--parallel 4` (batches 1–8) then `--parallel 6` (batches 9–11)

### 1.2 Timeout glossary — what each cap means

The system enforces **five nested timeouts**. Each catches a different
failure mode; they are NOT redundant.

```
  ┌─────────────────────────────────────────────────────────────────────────┐
  │                Five Nested Timeouts (outer → inner)                     │
  │                                                                         │
  │  ① Subprocess timeout (harness)                                         │
  │     ARISE_SUBPROCESS_TIMEOUT_SECONDS = 7800s                            │
  │     Matrix runner kills `main.py run` child if not exited.              │
  │     Outer safety net. Source: experiments/shared/harness.py:245         │
  │                              │                                          │
  │                              ▼                                          │
  │  ② Per-run orchestration cap                                            │
  │     orchestration.max_run_duration_seconds                              │
  │     Hard stop for the full orchestration run.                           │
  │     A1/A2: 7200s · B1: 5400s · C1: 21600s                              │
  │                              │                                          │
  │                              ▼                                          │
  │  ③ Per-worker cap                                                       │
  │     worker.timeout — time budget for a single worker invocation         │
  │     (one claude_code / openhands subprocess).                           │
  │     A1/A2: 5400s · B1: 3600s · C1: 5400s                               │
  │                              │                                          │
  │                              ▼                                          │
  │  ④ Per-step hard cap + watchdog                                         │
  │     orchestration.concurrency.max_agent_step_seconds                    │
  │     Hard cap per agent step. Stale-agent watchdog fires at 1.5×.        │
  │     A1/A2/B1: 900s (watchdog 1350s) · C1: 1800s (watchdog 2700s)        │
  │     Source: core/application/execution_service.py:614,694               │
  │                              │                                          │
  │                              ▼                                          │
  │  ⑤ Per-LLM-iteration cap                                                │
  │     worker.max_iterations_per_run — tool-calling rounds per worker      │
  │     A1/A2: 2500 · B1/C1: 250                                            │
  │                                                                         │
  │  (separate) orchestration.tool_calling.*.max_iterations = 20            │
  │     caps boss/manager/pending recon ROUNDS, not individual probes.      │
  └─────────────────────────────────────────────────────────────────────────┘
```

### 1.3 How a run gets labeled at matrix level

`main.py run` writes `run_manifest.json` with
`exit_status ∈ {success, timeout, failed}`. `run_matrix.py` reads that
field and maps it 1:1 onto the matrix summary's per-job status.

---

## 2. System model — phase decomposition and worker roles

Per `prompts/domains/secbench/assess.j2` (the operative manager prompt;
`prompts/domains/secbench/manager/*.j2` is dead-code on the happy path),
the boss decomposes the top-level CVE task into **exactly 4 ordered phases**,
each of which decomposes further into a fixed, named chain of leaf workers.
Every role name is bound verbatim — the prompt says **"copy verbatim"**
and **"Do NOT paraphrase"**.

### 2.1 Phase-and-role topology

```
  ┌──────────────────────────────────────────────────────────────────────────┐
  │                              BOSS                                        │
  │                 (decomposes into exactly 4 phase managers)               │
  │                                                                          │
  │   ─►  [Builder]  ──►  [Exploiter]  ──►  [Fixer]  ──►  [Reporter]        │
  │       depends_on:[]   depends_on:[0]    depends_on:[1]  depends_on:[0,1,2]│
  │                                                                          │
  ├──────────────┬─────────────────────┬─────────────────────┬───────────────┤
  │              │                     │                     │               │
  │  [Builder]   │  [Exploiter]        │  [Fixer]            │  [Reporter]   │
  │  3 workers   │  6 workers          │  6 workers          │  1 worker     │
  │  (chain)     │  (chain)            │  (chain)            │  (atomic)     │
  │  ┌────────┐  │  ┌────────────────┐ │  ┌────────────────┐ │  ┌─────────┐  │
  │  │ Build- │  │  │ PoC-Researcher │ │  │ Root-Cause-    │ │  │Reporter │  │
  │  │ Setup  │  │  └───────┬────────┘ │  │ Analyst        │ │  └─────────┘  │
  │  └───┬────┘  │          ▼          │  └───────┬────────┘ │               │
  │      ▼       │  ┌────────────────┐ │          ▼          │               │
  │  ┌────────┐  │  │ Data-Flow-     │ │  ┌────────────────┐ │               │
  │  │ Build- │  │  │ Analyst        │ │  │ Candidate-     │ │               │
  │  │Compiler│  │  └───────┬────────┘ │  │ Reviewer       │ │               │
  │  └───┬────┘  │          ▼          │  └───────┬────────┘ │               │
  │      ▼       │  ┌────────────────┐ │          ▼          │               │
  │  ┌────────┐  │  │ PoC-Tester     │ │  ┌────────────────┐ │               │
  │  │ Build- │  │  └───────┬────────┘ │  │ Regression-    │ │               │
  │  │Verifier│⬛│          ▼          │  │ Tester         │ │               │
  │  └────────┘  │  ┌────────────────┐ │  └───────┬────────┘ │               │
  │              │  │ Forward-       │ │          ▼          │               │
  │              │  │ Instrumentator │ │  ┌────────────────┐ │               │
  │              │  └───────┬────────┘ │  │ Patch-Creator  │ │               │
  │              │          ▼          │  └───────┬────────┘ │               │
  │              │  ┌────────────────┐ │          ▼          │               │
  │              │  │ Repro-Creator  │ │  ┌────────────────┐ │               │
  │              │  └───────┬────────┘ │  │ Patch-         │ │               │
  │              │          ▼          │  │ Validator      │⬛│               │
  │              │  ┌────────────────┐ │  └───────┬────────┘ │               │
  │              │  │ Exploit-       │⬛│          ▼          │               │
  │              │  │ Validator      │ │  ┌────────────────┐ │               │
  │              │  └────────────────┘ │  │ Fix-Aggregator │ │               │
  │              │                     │  └────────────────┘ │               │
  └──────────────┴─────────────────────┴─────────────────────┴───────────────┘

  ⬛ = terminal validator role for the phase (mandatory)
  Total happy-path leaf workers: 3 + 6 + 6 + 1 = 16

  Per assess.j2:11-19 — ALL roles in a phase chain are mandatory, not
  just the terminal validator. There is no "optional intermediate role"
  semantics in the current prompt.
```

### 2.2 Per-role responsibilities

**Builder phase** — infrastructure setup (not code analysis); 3 workers.

| Order | Role | Responsibility |
|---|---|---|
| 1 | Build-Setup | Identify base commit; prepare sanitizer-aware build environment; install required packages |
| 2 | Build-Compiler | Run sanitizer-instrumented build (ASan flags preserved); fix compile errors; verify ASan runtime is linked |
| 3 | **Build-Verifier** | **Terminal validator.** Confirm `/testcase/base_commit_hash`, `/src/build.sh`, `/testcase/repo_changes.diff`, `/testcase/packages.txt` all exist and the resulting binary runs |

**Exploiter phase** — PoC discovery and reproduction; 6 workers.

| Order | Role | Responsibility |
|---|---|---|
| 1 | PoC-Researcher | Find existing PoC in bug report (download-first) or craft minimal one; map each PoC operation → source function in `/testcase/poc_operation_map.txt` |
| 2 | Data-Flow-Analyst | Trace input flow from PoC parsing to crash site; identify buffer sizes, boundary conditions, type assumptions |
| 3 | PoC-Tester | Validate PoC against vulnerable binary using Valgrind + GDB + strace + ASan (cross-tool verification) |
| 4 | Forward-Instrumentator | Inject `fprintf` probes into pre-crash functions (using `poc_operation_map.txt`) to find the data-corruption origin — NOT the crash site. Output: `/testcase/forward_instrumentation.log` |
| 5 | Repro-Creator | Write standalone `/testcase/repro.sh` that triggers the reported sanitizer error |
| 6 | **Exploit-Validator** | **Terminal validator.** Run `repro.sh` 3× for deterministic reproduction; cross-check sanitizer type / crash address / source function against the bug description |

**Fixer phase** — root cause and patch; 6 workers.

| Order | Role | Responsibility |
|---|---|---|
| 1 | Root-Cause-Analyst | Identify true root cause via runtime instrumentation + static analysis; write machine-readable handoff block to `/testcase/root_cause_analysis.txt`: `PROPOSED_FIX_SITE`, `ALTERNATIVE_SITE`, `RUNTIME_TYPE_TAG`, plus rationale and rejection reasons |
| 2 | Candidate-Reviewer | Evaluate ≥2 candidate fix layers against a rubric: contract / coverage / failure-mode / false-positive risk |
| 3 | Regression-Tester | Run the existing project test suite against candidate patches |
| 4 | Patch-Creator | Produce minimal `/testcase/model_patch.diff`. **No scope creep**: only the change that fixes this CVE; no unrelated NULL-checks or defensive hardening |
| 5 | **Patch-Validator** | **Terminal validator.** Apply patch → re-run `repro.sh` → verify sanitizer error gone AND no new issues. Write canonical `/testcase/patch_validation_results.txt` block |
| 6 | Fix-Aggregator | Big-picture sanity check across all upstream workers; final consistency review |

**Reporter phase** — synthesis; 1 worker (no further decomposition).

| Order | Role | Responsibility |
|---|---|---|
| 1 | Reporter | Read all `/testcase/` artifacts and sibling context; synthesize `/testcase/security_report.md` (executive summary, vulnerability details, root cause, PoC evidence, fix implementation, validation results, tools used) |

### 2.3 Open design question — strict chain vs optional roles

A working assumption circulated in design is that **intermediate roles
should be *optional*** (the phase manager picks which to spawn) and only
the **terminal validator** is mandatory — e.g. Exploiter could spawn
just `PoC-Researcher → Exploit-Validator` for simple CVEs and add the
other 4 only when needed.

This is **NOT what the current `assess.j2` prompt says.** The prompt
binds the full chain verbatim ("Do NOT paraphrase", "exactly 6 workers").
There is a secondary inconsistency: `prompts/domains/secbench/boss.j2:58-60`
says "Exploiter / Fixer decompose into 4-5 workers" — but that file is
dead-code on the happy path; `assess.j2` is operative.

Reconciling this is an open question for next iteration: either (a) relax
`assess.j2` so non-validator roles become optional (matches the design
intent and would significantly cut worker count for simple CVEs), or
(b) codify the strict chain as the documented architecture and remove
the contradictory "optional" framing from internal discussion.

---

## 3. Success / failure criteria

Two distinct levels. **Mechanical** (does the file exist?) and
**semantic** (is the file *correct*?).

### 3.1 System-level outcome — coarse

`success` / `timeout` / `failed`, decided by the run's terminal event
(`WorkCompleted` vs `WorkFailed`) and persisted to `run_manifest.json`.
A run that finishes 4/4 phases counts as `success` **regardless of
whether the patch is correct** — so this is coarse.

### 3.2 Basic criteria (per-task deliverables; mechanical)

Each role writes one named artifact. Presence-of-file is the criterion.

| Role      | Deliverable | Path |
|-----------|-------------|------|
| Builder   | ASan-instrumented binary (+ `build.sh`, `base_commit_hash`, `repo_changes.diff`) | `/work/bin/<name>`, `/src/build.sh`, `/testcase/{base_commit_hash,repo_changes.diff}` |
| Exploiter | PoC file + reproduction script | `/testcase/poc.*`, `/testcase/repro.sh` |
| Fixer     | Unified-diff patch | `/testcase/model_patch.diff` |

Basic criteria are cheap to score: walk `runs/<run_id>/testcase/` after
the run and check `os.path.exists`.

### 3.3 Semantic criteria (does the artifact actually work?)

```
  ┌───────────────────────────────────────────────────────────────────────────┐
  │  Role       │ Semantic question                  │ State of evidence      │
  ├─────────────┼────────────────────────────────────┼────────────────────────┤
  │  Builder    │ Does the binary build cleanly AND  │ Transitive only        │
  │             │ run?                                │ (Exploiter invokes it) │
  ├─────────────┼────────────────────────────────────┼────────────────────────┤
  │  Exploiter  │ Does repro.sh produce the SAME     │ ⚠ NEEDS INVESTIGATION  │
  │             │ sanitizer error as the CVE bug      │ No unified report file │
  │             │ description?                        │ (see §3.1)             │
  ├─────────────┼────────────────────────────────────┼────────────────────────┤
  │  Fixer      │ Apply model_patch.diff → re-run    │ ✓ patch_validation_    │
  │             │ repro.sh → sanitizer error gone?    │   results.txt (canonical│
  │             │                                     │   file, 95/115 B1 runs)│
  └─────────────┴────────────────────────────────────┴────────────────────────┘
```

**Exploiter semantic criterion is currently un-comparable across cells.**
We do not have a canonical, parseable post-exploit verification file —
workers wrote ad-hoc filenames. This must be resolved before we can quote
exploiter pass-rates in the paper.

**Fixer semantic criterion is in good shape.** `patch_validation_results.txt`
exists in a machine-readable form for 95/115 B1 runs.

---

## 4. Progress (what got done)

- **A1, A2 ran to completion** on the 244-CVE declared dataset; 194
  runs executed (81 A1 + 113 A2) and recorded in the event store. Per-run
  metrics, per-cell breakdown, and paired A1-vs-A2 per-CVE deltas all
  extracted to CSV.
- **B1 ran to completion** across 124 runs (122 CVEs + 11 credit-failure
  reruns). Library-by-library breakdown produced; system-level success
  rate **65.3 %** (81/124), timeout **19.4 %** (24/124), failed
  **15.3 %** (19/124).
- **Two systemic prompt bugs discovered in B1**:
  1. `exploit_validation_results.txt` is missing from
     `prompts/domains/secbench/worker/exploiter.j2` deliverables — only
     1/115 B1 runs wrote it (workers improvised 15+ alternate filenames
     instead).
  2. `prompts/domains/secbench/manager/*.j2` templates are dead code on
     the happy path (PENDING → `assess_task` → `decompose` bypasses
     `evaluate_task` entirely). Role-specific deliverable contracts must
     live in `worker/*.j2`, not `manager/*.j2`.
- **First cross-cell setup snapshot written**:
  `research/05-experiment-execution-summary.md` — every cell, every
  timeout cap, every deliverable contract grounded to source files. This
  is the authoritative reference for paper methodology section.

---

## 5. Results: A1 vs A2 vs B1

Data extracted from the two authoritative analysis reports:

- A1/A2 — `experiments/a12-batch-autogen/reports/a12_claude_code_cli_report.md` (generated by `experiments/shared/scripts/a12_analysis.py`).
- B1 — `experiments/b1-batch-autogen/reports/b1_analysis_report.md` (generated by `experiments/shared/scripts/b1_analysis.py`).

A1 and A2 land within 4 percentage points of each other on terminal success
(78.5% vs 82.1%) and at ~$1.80 per successful run. **B1 is meaningfully worse
on terminal success (65.9%) and ~5× more expensive per success ($9.83)** —
and shows the BOSS → MANAGER → WORKER architecture *spending* in different
ways (2.5× the tool calls, 3.3× the recon, 11× the security-tool calls per
run) without translating that effort into more terminal successes. Full
real-pipeline success is `0` for B1 by construction (no
`exploit_validation_results.txt` is written — see §6.1), so that column is
not comparable to A1/A2.

### 5.1 Cell-level results (holistic view)

| Metric                       |     A1 |     A2 |        B1 |
|------------------------------|-------:|-------:|----------:|
| Enrolled runs                |    122 |    113 |       123 |
| Terminal runs                |    121 |    112 |       123 |
| Successful terminal runs     |     95 |     92 |        81 |
| Terminal success rate        |  78.5% |  82.1% |     65.9% |
| Full real-pipeline success   |     51 |     48 |    **0**¹ |
| Total observed cost          |$182.63 |$155.52 |  $796.04  |
| Cost / success               |  $1.92 |  $1.69 |     $9.83 |
| Median duration (s)          |  891.2 |  828.1 |**4,211.5**|
| Total tokens                 |   336M |   283M |      890M |

¹ B1's `exploiter_real_success` is `0` by construction (the orchestrator
never writes `testcase/exploit_validation_results.txt`), so the AND-chained
`real_pipeline_success` is also `0` for every B1 run. **This is a metric
artifact, not a failure mode.** See §6.1 for the underlying prompt bug.

**Cost attribution:**

| Cell | Manager cost | Worker cost | Cost-observed runs | Positive-cost runs |
|------|-------------:|------------:|-------------------:|-------------------:|
| A1   |        $0.00 |     $182.63 |                113 |                105 |
| A2   |        $0.00 |     $155.52 |                108 |                101 |
| B1   |      $232.74 |     $563.29 |                116 |                116 |

A1/A2 attribute no cost to the manager because the flat CLI bypasses a
separate manager LLM. B1's $232.74 manager cost is the BOSS + MANAGER LLM
calls that drive decomposition.

**Result-evidence (mechanical: file present?):**

| Cell | Builder real / N | `work/bin` Exec | Fix Files | Model Patches | Repro Scripts | Security Reports |
|------|-----------------:|----------------:|----------:|--------------:|--------------:|-----------------:|
| A1   |          105/122 |               — |       105 |           106 |           106 |              105 |
| A2   |          101/113 |               — |        97 |           102 |           102 |               97 |
| B1   |       **61/123** |               — |        49 |            98 |           103 |               85 |

B1's builder pass rate (49.6%) is well below A1's (86.1%) or A2's (89.4%).
Across every deliverable, B1 trails A1/A2.

**Result-evidence (semantic: verdict + apply + build + post-error none):**

B1's metric cannot be measured due to misleading artifact generation prompt. Don't trust B1 cell. 

| Cell | Patch verdict PASS | Patch apply clean | Patch build success | Patch post-error none |
|------|-------------------:|------------------:|--------------------:|----------------------:|
| A1   |                100 |               105 |                 105 |                    98 |
| A2   |                 90 |                97 |                  96 |                    89 |
| B1   |              **0** |             **0** |               **0** |                 **0** |

B1's patch-verdict rate is `0` across the 49 `patch_validation_results.txt`
files that exist on disk — `VERDICT == PASS` was never set. The failure is
upstream of the validator in this batch.

### 5.2 Deep dive

#### 5.2.1 Paired inference (A1 vs A2)

B1 has no pair cell, so this is A1 ↔ A2 only (111 matched
`(task, replicate)` pairs). p-values from sign / exact-McNemar tests
(`_paired_metric_stats` / `_paired_binary_stats`).

| Metric                | N   | Mean A1 | Mean A2 | Mean Δ |  Sign p  |
|-----------------------|----:|--------:|--------:|-------:|---------:|
| Success rate          | 111 |   79.3% |   82.0% |  -2.7% |   0.6776 |
| Total cost            | 101 |   $1.62 |   $1.34 |  $0.28 |   0.0495 |
| Duration (s)          | 111 |   938.6 |   883.8 |   54.8 |   0.0132 |
| Tokens                | 101 |   2.96M |   2.50M |  0.46M |   0.0495 |
| Total tool calls      | 111 |    94.3 |    83.2 |   11.1 |   0.0138 |
| Task-family calls     | 111 |     9.8 |     1.8 |    8.0 | &lt;0.0001  |
| Subagent-spawn calls  | 111 |     0.0 |     0.0 |    0.0 |   0.2500 |
| Cheat calls           | 111 |     3.6 |     3.7 |   -0.0 |   0.8243 |
| Recon calls           | 111 |    55.3 |    51.7 |    3.5 |   0.0619 |
| Security-tool calls   | 111 |     2.7 |     2.1 |    0.7 |   0.4767 |

| Outcome                | N   | A1-only | A2-only | Both | Neither |    Δ   | Exact p |
|------------------------|----:|--------:|--------:|-----:|--------:|-------:|--------:|
| Builder real success   | 111 |       4 |       7 |   92 |       8 |  -2.7% |  0.5488 |
| Exploiter real success | 111 |      18 |      22 |   36 |      35 |  -3.6% |  0.6358 |
| Fixer real success     | 111 |      10 |      10 |   78 |      13 |   0.0% |  1.0000 |
| Full real pipeline     | 111 |      19 |      21 |   27 |      44 |  -1.8% |  0.8746 |

**Takeaway**: A1 ≠ A2 on tool *usage* — A1 averages ~8 more task-family
calls per run (`p < 0.0001`) and ~11 more total tool calls (`p = 0.014`),
driven by Task subagent availability. **A1 ≈ A2 on outcomes** — none of
the four binary outcomes (builder / exploiter / fixer / full) differ at
`p < 0.05`. The Task subagent feature increased work effort but did not
move pass rates. A2 was significantly faster (`p = 0.013`) and cheaper
(`p = 0.050`).

#### 5.2.2 Tool use (+ tool-call breakdown)

Averages per run (`sum / N enrolled`):

| Cell | N   | Total Tools | Cheat | Recon     | Security | Subagent | Task-Family | TaskCreate | TaskUpdate | Shell    | File Read | File Write | File Edit | Search |
|------|----:|------------:|------:|----------:|---------:|---------:|------------:|-----------:|-----------:|---------:|----------:|-----------:|----------:|-------:|
| A1   | 122 |        91.9 |   3.5 |      53.7 |      2.6 |     0.02 |        10.0 |        2.7 |        6.7 |     59.2 |       9.4 |        6.2 |       2.3 |    4.9 |
| A2   | 113 |        82.8 |   3.6 |      51.6 |      2.0 |     0.00 |         1.8 |        0.5 |        1.2 |     57.2 |       9.8 |        6.9 |       2.5 |    4.5 |
| B1   | 123 |   **234.3** |   3.6 | **176.7** | **29.0** |     0.00 |         0.0 |        0.0 |        0.0 |**160.2** |  **37.6** |   **15.1** |   **7.1** |    5.7 |

B1 dwarfs A1/A2 across the board. The recursive orchestrator does 2.5×
the tool calls, 3.3× the recon, 11× the security-tool usage per run.

**B1 hierarchical breakdown** (depth in `AgentCreated.parent_id` chain
— BOSS = depth 0, MANAGER = depth 1, WORKER = depth ≥ 2):

| Role    | Nodes Total | Tool Calls Total | Mean / Node | Mean / Run |
|---------|------------:|-----------------:|------------:|-----------:|
| BOSS    |         123 |            **0** |        0.00 |       0.00 |
| MANAGER |         464 |            3,852 |        8.30 |      31.32 |
| WORKER  |       1,194 |           24,967 |       20.91 |     202.98 |

The `0` BOSS tool calls is correct under the metric definition
(`ThoughtCaptured(output_type='tool_use')`). The boss does its
decomposition via structured output (`SubtasksDefined` event) rather
than a tool call, and emits only one `PromptSent` per run.

#### 5.2.3 Cheat analysis

Shell commands that read git history or history-bearing refs. Same code
path across all three cells (`a12_analysis.compute_tool_audit` →
`cheating_detector._cheating_pattern`).

| Pattern                  | A1 calls | A1 avg | A1 runs | A2 calls | A2 avg | A2 runs | B1 calls | B1 avg | B1 runs |
|--------------------------|---------:|-------:|--------:|---------:|-------:|--------:|---------:|-------:|--------:|
| Any cheat call           |      428 |  3.508 |     106 |      411 |  3.637 |     104 |      444 |  3.610 |     105 |
| `git log`                |      376 |  3.082 |     106 |      365 |  3.230 |     104 |      334 |  2.715 |     103 |
| `git show`               |       37 |  0.303 |      18 |       32 |  0.283 |      14 |       13 |  0.106 |       8 |
| `git reflog`             |        0 |  0.000 |       0 |        0 |  0.000 |       0 |        0 |  0.000 |       0 |
| `git diff <history/ref>` |       15 |  0.123 |      13 |       14 |  0.124 |      12 |       97 |  0.789 |      80 |

Cheat *rates* are remarkably similar (3.5–3.6 per run; ~85% of runs touch
git history). The *composition* differs: A1/A2 lean on `git log` (88% of
cheats); B1 leans more on `git diff <history/ref>` (22% of cheats vs ~3%
in A1/A2). The recursive orchestrator's workers more often diff a specific
revision than scan the log.

#### 5.2.4 Bash analysis

SHELL-classified tool calls partitioned by first-match-wins regex
priority (`classify_bash_command`):

| Cell | Bash Recon | Bash Exploit | Bash Security Scan | Bash Build | Bash Test/Fuzz Exec | Bash Git | Bash Other |
|------|-----------:|-------------:|-------------------:|-----------:|--------------------:|---------:|-----------:|
| A1   |      4,808 |            0 |                316 |        521 |                   4 |    1,200 |      5,414 |
| A2   |      4,200 |            0 |                240 |        433 |                   9 |    1,201 |      4,778 |
| B1   | **16,401** |            0 |          **2,554** |        778 |                   0 |    1,602 | **17,249** |

Caveats: `Bash Recon` and `Bash Security Scan` double-count with `Bash
Other` by design (regex-secondary-counter columns — see the source report
template prose for the full semantics). `Bash Exploit = 0` everywhere is
consistent: no `sqlmap` / `metasploit` / `pwntools` invocation in any cell.

### 5.3 Failure analysis

`WorkFailed` classifications at the boss aggregate. Login/auth and
credit/quota failures are excluded from phase attribution.

**Excluded operational causes:**

| Cause                  | A1 | A2 |     B1 |
|------------------------|---:|---:|-------:|
| Login/authentication   |  7 |  7 |      0 |
| Credit/quota           |  1 |  0 | **13** |

B1 hit credit-quota failures 13 times — these are the rerun-eligible
failures already noted in the cohort accounting.

**Terminal status counts:**

| Status      | A1 | A2 | B1 |
|-------------|---:|---:|---:|
| completed   | 95 | 92 | 81 |
| in_progress |  1 |  1 |  0 |
| timed_out   | 17 |  9 | 24 |
| failed      |  9 | 11 | 18 |

**Non-success classification** (boss `failure_mode`):

| Classification     | A1 | A2 |     B1 |
|--------------------|---:|---:|-------:|
| inactivity_timeout | 17 |  9 |      0 |
| provider_failure   |  1 |  4 |      6 |
| work_error         |  8 |  7 | **33** |

B1 produces 4× more `work_error` than A1/A2 and zero `inactivity_timeout`
— the tighter B1 per-step budget appears to surface errors as
`work_error` before the inactivity watchdog fires.

**Failure phase** (first missing artifact: builder → exploiter → fixer →
reporter; provider/api separate):

| Phase                       | A1 | A2 |     B1 |
|-----------------------------|---:|---:|-------:|
| builder                     |  8 |  1 |     15 |
| exploiter                   |  4 |  4 | **11**¹|
| fixer                       |  1 |  3 |      0 |
| reporter                    |  0 |  0 |      0 |
| provider/api                |  1 |  4 |      0 |
| post-evidence orchestration |  4 |  1 |      0 |

¹  **Do not compare this number to A1/A2's exploiter counts** until the exploiter-prompt fix.

### 5.4 Per-run breakdown

The per-run tables (122 + 113 + 123 = **358 rows** total) are not inlined
here. They live in:

- `experiments/a12-batch-autogen/reports/a12_claude_code_cli_report.md` — sorted by status (completed → in_progress → timed_out → failed).
- `experiments/b1-batch-autogen/reports/b1_analysis_report.md` — same sort.
- Full CSVs at `experiments/{a12,b1}-batch-autogen/reports/{a12_claude_code_cli,b1_analysis}_metrics.csv` (one row per run with the full ~60-column schema).

Each row carries: status, CVE, cell, run id, cost, the tool-call
breakdown (actual / cheat / recon / security / subagent / task-family /
TaskCreate / TaskUpdate / TaskList), and the four binary outcomes
(builder / exploiter / fixer / full pipeline).

---

## 6. Blockers

### 6.1 Exploiter has no unified report format

**Impact**: semantic exploiter pass-rate is **not directly comparable
across cells** until either:

- the worker prompt is patched and B1 is re-run, OR
- a per-run heuristic extracts the verdict from the 15+ variant filenames.

**Source files to fix**:

- `prompts/domains/secbench/worker/exploiter.j2` — add canonical
  `/testcase/exploit_validation_results.txt` to deliverables section.
- Optional: mirror block format from `worker/fixer.j2:130` so the file
  is machine-parseable.

### 6.2 C1 (Qwen) failed; cause un-investigated

No `reports/` or `results/` written for C1; no rows in the event store
for that study. Possibilities to triage (none verified yet): local
Ollama daemon configuration, the 6-hour `max_run_duration_seconds`
producing host-saturation, worker-tool wiring issues with the
OpenHands + Qwen combination on Mac. Configs and dataset are pinned for
reproducibility once we know what broke.

---

## 7. Plan for next week

1. **Patch the exploiter prompt** (canonical
   `exploit_validation_results.txt` filename + machine-readable block)
   and either re-run B1 or implement a variant-filename heuristic, so
   exploiter semantic pass-rate becomes comparable.
2. **Investigate and reattempt C1.** Triage the failure cause, then
   re-run with whatever fix is needed.
3. **Pull basic-criteria pass rates** (builder binary present /
   exploiter PoC+repro present / fixer model_patch.diff present) into
   per-cell columns for A1/A2/B1.
4. **Pull `patch_validation_results.txt` PASS/FAIL** into per-cell
   columns from the 95/115 B1 runs.
5. Start drafting the paper's methodology section against
   `research/05-experiment-execution-summary.md`.

---
