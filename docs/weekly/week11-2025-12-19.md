# Research Sync 2025-12-19

**Attendees:** Caroline Cahill, In Keun Kim, Song Li

**Date:** 2025-12-19
---

## Summary

Significant engineering progress this sprint. We implemented a comprehensive budget management system with reward/penalty mechanisms, cost tracking with real-time visualization, and merged both feature branches. The codebase can be viewed [here](https://github.com/arise-ai-security/arise-sec-lion).

**Current Status:** 🟢 On Track / 🟡 Minor Obstacles / 🔴 Blocked

🟢 **On Track**: Core budget allocation, multi-model subordinate spawning, and verification systems are implemented. Cost tracking dashboard is functional.

🟡 **Minor Obstacles**: Branch divergence required significant merge effort. Some integration testing pending.

🔴 **Blocked**: None.

---

## Key Accomplishments This Week

### 1. Feature List: What This System Can Do

| Feature | Description | Status |
|---------|-------------|--------|
| **Budget Allocation** | Agents receive budget from parents, track spending, return remaining with reward/penalty ratios | ✅ Complete |
| **Multi-Model Subordinates** | Spawn 3 agents with different LLMs (Claude, Gemini, GPT-4o) for same subtask - first success wins | ✅ Complete |
| **Verification Heuristics** | Automatic verification injection based on complexity, random probability, suspicious reports | ✅ Complete |
| **Task Queue** | FIFO queue per agent for subtask management with retry support | ✅ Complete |
| **Cost Tracking** | TokensConsumed/WorkerCostRecorded events track actual LLM spending | ✅ Complete |
| **Real-time Dashboard** | SSE-powered cost panel showing breakdown by role, model, operation | ✅ Complete |
| **Execution Summary API** | `/api/agents/{id}/execution-summary` with comprehensive metrics | ✅ Complete |
| **Property-based Testing** | Hypothesis tests ensuring cost invariants (sums equal totals) | ✅ Complete |

### 2. Results: What Happens When We Execute

**Agent Lifecycle Flow:**
```
BOSS (budget=1000)
  ├─ Spawns 3 subordinates per subtask (budget split: 333 each)
  │   ├─ Claude Sonnet → succeeds first → wins
  │   ├─ Gemini Pro → terminated (sibling won)
  │   └─ GPT-4o → terminated (sibling won)
  │
  ├─ Winner returns budget with reward (111 * 1.2 = 133.2)
  ├─ Verification heuristic check (10% random, complexity-based)
  │   └─ If triggered: spawn verifier agent
  │
  └─ Process next subtask from queue
```

**Cost Dashboard Shows:**
- Total cost breakdown (LLM vs Worker)
- Cost by role (BOSS: $0.10, MANAGER: $0.25, WORKER: $1.50)
- Cost by model (gpt-4o: $0.80, claude-sonnet: $1.05)
- Real-time updates via Server-Sent Events

### 3. Engineering Improvements

| Area | Before | After |
|------|--------|-------|
| Settings | Flat structure, mixed secrets/config | Nested groups (database, llm, worker, orchestration) |
| Docker | Manual env setup | Explicit defaults matching config.yaml |
| CQRS | Projections in application | Moved to `core/query/` for clean separation |
| API | Basic agent endpoints | Added execution-summary, SSE streaming |
| Frontend | Static display | Real-time cost panel with SSE updates |

### 4. Domain Events Added

**Budget System (experiment branch):**
- `BudgetAllocated`, `BudgetAdjusted`, `BudgetRecollected`
- `TaskEnqueued`, `TaskDequeued`
- `AgentTerminated`, `SubtreeAborted`
- `VerificationInjected`, `VerifierSpawned`, `VerificationCompleted`
- `SubordinatesSpawned`, `FirstSuccessRecorded`, `AllSubordinatesFailed`

**Cost Tracking (develop branch):**
- `TokensConsumed` (LLM calls with token counts)
- `WorkerCostRecorded` (worker tool execution costs)
- `BudgetExceeded`, `LimitEnforced`

### 5. Other Observations (Brainstormed)

**Architecture Strengths:**
- Event sourcing enables full audit trail of budget flow
- Ports & adapters pattern makes reward mechanism pluggable
- Property-based tests catch edge cases (floating point precision)

**Potential Research Directions:**
1. **Adaptive Reward Ratios**: Learn optimal reward/penalty ratios from historical success rates
2. **Model Selection Strategy**: Use task complexity to choose which models to spawn
3. **Budget Prediction**: Estimate subtask budget requirements before allocation
4. **Verification Scheduling**: Optimize when to inject verification vs trust results

**Technical Debt to Address:**
- Integration tests for budget flow end-to-end
- Frontend tests for CostPanel component
- Documentation for new ports (RewardMechanismPort, VerificationHeuristicsPort)

### 6. Next Steps

**Engineering:**
1. **Integrate Google ADK, Claude Code** - Replace OpenHands dependency with stable alternatives
2. **System-level hyperparameter tuning** - Configurable reward ratios, verification thresholds, budget splits
3. **Prompt engineering per node class** - Optimize prompts for BOSS, MANAGER, WORKER roles

**Cyber Security Application:**
1. **Fix master prompt for BOSS node** - Adapt delegation prompt for security context (vulnerability analysis, threat modeling)
2. **Baseline experiment with SecBench** - Run benchmark suite to establish performance metrics
